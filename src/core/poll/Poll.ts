import {Interaction, CommandInteraction, Message} from 'discord.js';
import Embedder from '../embedder/Embedder';



export default class Poll {
    static start(options: any, int: CommandInteraction, msg: Message) {
        let max = options.length;

    }
}